export const xorEncryptDecrypt=(input,secretKey)=>
{
    let output='';
    for(let i=0;i<input.length;i++)
    {
        const inputcode=input.charCodeAr(i);
        const secretcode=secretKey.charcode
    }
}